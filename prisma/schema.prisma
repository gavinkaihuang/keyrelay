generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Key {
  id            String     @id @default(uuid()) @db.Uuid
  platform_id   String     @db.Uuid
  name          String
  secret_key    String
  key_preview   String?
  status        String     @default("active")
  last_used_at  DateTime   @default(dbgenerated("'1970-01-01 00:00:00+00'::timestamp with time zone"))
  cooling_until DateTime?
  last_error    String?
  fail_count    Int        @default(0)
  created_at    DateTime   @default(now())
  platform_ref  Platform   @relation(fields: [platform_id], references: [id], onDelete: Restrict)
  usage_logs    UsageLog[]

  @@index([platform_id])
  @@index([last_used_at])
  @@index([created_at])
  @@map("keys")
}

model Platform {
  id         String   @id @default(uuid()) @db.Uuid
  name       String   @unique
  icon       String?
  created_at DateTime @default(now())
  keys       Key[]

  @@index([created_at])
  @@map("platforms")
}

model UsageLog {
  id             String   @id @default(uuid()) @db.Uuid
  key_id         String   @db.Uuid
  project_name   String
  request_status String
  error_message  String?
  created_at     DateTime @default(now())
  key            Key      @relation(fields: [key_id], references: [id], onDelete: Cascade)

  @@index([key_id])
  @@map("usage_logs")
}
